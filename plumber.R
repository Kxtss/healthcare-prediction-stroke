# Generated by the vetiver package; edit with care

library(pins)
library(plumber)
library(rapidoc)
library(vetiver)

# Packages needed to generate model predictions
if (FALSE) {
    library(parsnip)
    library(ranger)
    library(recipes)
    library(themis)
    library(workflows)
}
b <- board_folder(path = "C:/Users/your_user/Desktop/stroke_api/model_board")
v <- vetiver_pin_read("stroke_prediction_rf", board = b)

#* @plumber
function(pr) {
    pr %>% vetiver_api(v)
}